// @ts-ignore
import Battery from '@tuya-miniapp/smart-ui/dist/battery/index';
import { withReactProps } from '../common/withReactProps';

export default withReactProps(Battery as React.ElementType);
